# Perceptron algorithm

## Installing modules
Numpy and Pandas modules required
```
pip install numpy
pip install pandas
```
## Modify file path
There are two variables train_path and test_path in the __main__ method, you need to assign your own path to the two variables or ensure the files are in the same directory as the .py file
